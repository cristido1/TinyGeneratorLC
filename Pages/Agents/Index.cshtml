@page
@model TinyGenerator.Pages.Agents.IndexModel
@{
    Layout = "_Layout";
    ViewData["Title"] = "Agents";
    ViewData["PageDescription"] = "Gestisci gli agenti e le loro configurazioni";
}

<div class="mb-3 d-flex justify-content-between align-items-center">
    <form method="get" class="d-flex align-items-center gap-2" asp-page="./Index" id="agentFilterForm">
        <input type="hidden" name="Page" value="1" />
        <input type="search" name="Search" id="searchBox" value="@Model.Search" class="form-control" placeholder="Search agents..." />
        <select name="ModelFilter" id="modelFilterSelect" class="form-select" style="width: 220px">
            <option value="" selected="@(string.IsNullOrWhiteSpace(Model.ModelFilter) ? "selected" : null)">All models</option>
            <option value="__none__" selected="@(Model.ModelFilter == "__none__" ? "selected" : null)">No model assigned</option>
            @foreach (var m in Model.Models)
            {
                var isSelected = !string.IsNullOrWhiteSpace(Model.ModelFilter) && string.Equals(Model.ModelFilter, m.Name, StringComparison.OrdinalIgnoreCase);
                <option value="@m.Name" selected="@(isSelected ? "selected" : null)">@m.Name @((!string.IsNullOrWhiteSpace(m.Provider)) ? "(" + m.Provider + ")" : "")</option>
            }
        </select>
        <button type="submit" class="btn btn-outline-primary">Search</button>
    </form>
    <a class="btn btn-primary" asp-page="./Create">Create Agent</a>
</div>

<table id="tabAgents" class="table table-bordered">
    <thead>
        <tr>
            <th>Name</th>
            <th>Role</th>
            <th>Model</th>
            <th>Temperature</th>
            <th>Top P</th>
            <th>Voice</th>
            <th>Skills</th>
            <th>JSON Format</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @if (Model.Agents != null && Model.Agents.Any())
    {
        foreach (var a in Model.Agents)
        {
                <tr>
                <td>@a.Name</td>
                <td>@a.Role</td>
                <td>@(string.IsNullOrWhiteSpace(a.ModelName) ? a.ModelId?.ToString() ?? "" : a.ModelName)</td>
                <td>@(a.Temperature.HasValue ? a.Temperature.Value.ToString("0.##") : "-")</td>
                <td>@(a.TopP.HasValue ? a.TopP.Value.ToString("0.##") : "-")</td>
                <td>@(string.IsNullOrWhiteSpace(a.VoiceName) ? (a.VoiceId?.ToString() ?? "") : a.VoiceName)</td>
                <td>@a.Skills</td>
                <td>@(string.IsNullOrWhiteSpace(a.JsonResponseFormat) ? "-" : a.JsonResponseFormat)</td>
                <td>
                    <a class="btn btn-sm btn-outline-primary me-1" asp-page="./Edit" asp-route-id="@a.Id">Edit</a>
                    @if (a.Role?.Equals("writer", StringComparison.OrdinalIgnoreCase) == true)
                    {
                        <button type="button" class="btn btn-sm btn-outline-success me-1 generate-story-btn" data-agent-id="@a.Id" data-agent-name="@a.Name">
                            <i class="bi bi-pencil-square"></i> Generate
                        </button>
                    }
                    <form method="post" asp-page-handler="Delete" asp-route-id="@a.Id" class="d-inline agent-delete-form">
                        <button type="button" class="btn btn-sm btn-outline-danger agent-delete-btn">Delete</button>
                    </form>
                </td>
            </tr>
        }
    }
    else
    {
        <tr><td colspan="9">No agents found.</td></tr>
    }
    </tbody>
</table>

@if (Model.TotalPages > 1)
{
    <nav>
        <ul class="pagination">
        @{
            var current = Model.Page;
            var total = Model.TotalPages;
            if (current > 1)
            {
                <li class="page-item">
                    <a class="page-link" asp-route-Page="@(current-1)" asp-route-Search="@Model.Search">Previous</a>
                </li>
            }
            for (int i = 1; i <= total; i++)
            {
                <li class="page-item @(i == current ? "active" : "")">
                    <a class="page-link" asp-route-Page="@i" asp-route-Search="@Model.Search">@i</a>
                </li>
            }
            if (current < total)
            {
                <li class="page-item">
                    <a class="page-link" asp-route-Page="@(current+1)" asp-route-Search="@Model.Search">Next</a>
                </li>
            }
        }
        </ul>
    </nav>
}

@section Styles {
    <link rel="stylesheet" href="~/css/site.css" />
}

@section Scripts {
    <script src="~/js/agents-index.js" asp-append-version="true"></script>
    <script>
        (function(){
            function initAgentsTable() {
                try {
                    var table = $('#tabAgents').DataTable({
                        responsive: true,
                        colReorder: true,
                        dom: 'Bfrtip',
                        buttons: [
                            'colvis'
                        ],
                        columnDefs: [ { orderable: false, targets: -1 } ],
                        order: [[0, 'asc']],
                        pageLength: 25,
                        stateSave: true,
                        stateSaveName: 'DataTables_agents',
                        stateDuration: -1
                    });

                    // If server-side pagination controls exist, hide them when DataTables is active
                    $('nav.pagination').hide();

                    // Wire existing search box to DataTables search
                    $('#searchBox').on('input', function(){ table.search(this.value).draw(); });
                    
                    // Handle delete buttons with SweetAlert2
                    document.querySelectorAll('.agent-delete-btn').forEach(btn => {
                        btn.addEventListener('click', async (e) => {
                            e.preventDefault();
                            const form = btn.closest('.agent-delete-form');
                            const confirmed = await showConfirm('Delete Agent', 'Are you sure you want to delete this agent?', 'Delete', 'Cancel');
                            if (confirmed) {
                                form.submit();
                            }
                        });
                    });

                    // Handle Generate Story buttons
                    const storyModalEl = document.getElementById('storyModal');
                    // Move modal to document.body to avoid stacking-context issues
                    try {
                        if (storyModalEl && storyModalEl.parentElement !== document.body) {
                            document.body.appendChild(storyModalEl);
                        }
                    } catch (e) {
                        console.warn('Failed to append modal to body', e);
                    }

                    const storyModal = new bootstrap.Modal(storyModalEl, {
                        backdrop: true,
                        keyboard: true
                    });
                    const storyThemeInput = document.getElementById('storyThemeInput');
                    const modalAgentId = document.getElementById('modalAgentId');
                    const STORAGE_KEY = 'lastStoryTheme';

                    // Ensure backdrop and modal z-index are above app overlay once shown
                    storyModalEl.addEventListener('shown.bs.modal', function() {
                        const backdrop = document.querySelector('.modal-backdrop');
                        if (backdrop) {
                            backdrop.style.zIndex = '10550';
                        }
                        storyModalEl.style.zIndex = '10600';
                        // focus textarea
                        if (storyThemeInput) storyThemeInput.focus();
                    });

                    // Load last theme from localStorage
                    const lastTheme = localStorage.getItem(STORAGE_KEY);
                    if (lastTheme && storyThemeInput) {
                        storyThemeInput.value = lastTheme;
                    }

                    document.querySelectorAll('.generate-story-btn').forEach(btn => {
                        btn.addEventListener('click', (e) => {
                            e.preventDefault();
                            const agentId = btn.dataset.agentId;
                            const agentName = btn.dataset.agentName;
                            if (modalAgentId) modalAgentId.value = agentId;
                            const titleEl = document.querySelector('#storyModal .modal-title');
                            if (titleEl) titleEl.textContent = `Generate Story - ${agentName}`;
                            storyModal.show();
                        });
                    });

                    // Save theme to localStorage on form submit
                    const storyForm = document.getElementById('storyForm');
                    if (storyForm) {
                        storyForm.addEventListener('submit', () => {
                            const theme = storyThemeInput ? storyThemeInput.value.trim() : '';
                            if (theme) {
                                localStorage.setItem(STORAGE_KEY, theme);
                            }
                        });
                    }

                    const modelSelect = document.getElementById('modelFilterSelect');
                    if (modelSelect && modelSelect.form) {
                        modelSelect.addEventListener('change', () => {
                            modelSelect.form.submit();
                        });
                    }
                } catch (e) {
                    console.warn('Failed to initialize Agents DataTable', e);
                }
            }
            if (document.readyState === 'loading') {
                document.addEventListener('DOMContentLoaded', initAgentsTable);
            } else { initAgentsTable(); }
        })();
    </script>
}

<!-- Story Generation Modal -->
<div class="modal fade" id="storyModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Generate Story</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <form method="post" asp-page-handler="GenerateStory" id="storyForm">
                <div class="modal-body">
                    <input type="hidden" name="AgentId" id="modalAgentId" />
                    <div class="mb-3">
                        <label for="storyThemeInput" class="form-label">Story Theme</label>
                        <textarea class="form-control" id="storyThemeInput" name="StoryTheme" rows="3" placeholder="Describe the story theme..." required></textarea>
                        <div class="form-text">What should the story be about?</div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="submit" class="btn btn-primary">Generate Story</button>
                </div>
            </form>
        </div>
    </div>
</div>
