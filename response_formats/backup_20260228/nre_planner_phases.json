{
  "type": "object",
  "properties": {
    "phases": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "index": { "type": "integer", "minimum": 1 },
          "name": { "type": "string" },
          "objective": { "type": "string" },
          "conflict": { "type": "string" },
          "tension_level": { "type": "integer", "minimum": 1, "maximum": 5 }
        },
        "required": ["index", "name", "objective", "conflict", "tension_level"],
        "additionalProperties": false
      }
    }
  },
  "required": ["phases"],
  "additionalProperties": false
}
