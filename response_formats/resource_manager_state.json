{
  "type": "object",
  "additionalProperties": false,
  "required": ["updated_resources"],
  "properties": {
    "updated_resources": {
      "type": "array",
      "description": "Solo le risorse che hanno cambiato stato rispetto al canon_state corrente.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name"],
        "properties": {
          "name": { "type": "string", "description": "Nome risorsa da aggiornare." },
          "status_flag": {
            "type": "string",
            "description": "Stato sintetico della risorsa.",
            "enum": ["active", "damaged", "critical", "destroyed", "missing", "inactive", "depleted", "captured", "lost", "unavailable"]
          },
          "quantity": { "type": "integer", "minimum": 0, "description": "Valore quantitativo principale della risorsa (alternativa a integrity_percent)." },
          "integrity_percent": { "type": "integer", "minimum": 0, "maximum": 100, "description": "Integrita percentuale della risorsa (alternativa a quantity)." },
          "notes_json": { "type": ["object", "null"], "description": "Metadati aggiuntivi opzionali in forma strutturata." }
        }
      }
    }
  }
}
